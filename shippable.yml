language: none

build:

  pre_ci_boot:
    image_name: drydock/u14
    image_tag: v6.1.4
    pull: true
    
  ci:  
    - python -V
    - pip --version
    - aws --version
    - eb --version
    - gcloud version
    - jfrog --version
   # - kubectl version
    - packer version
    - terraform --version
    - apt search azure-cli
    - az --version
    #- yarn --version
    #- echo "-------->PACKER_VERSION=$(packer version)"
    #- echo "-------->TERRAFORM_VERSION=$(terraform --version)"
    #- echo "-------->KUBERNETES_VERSION=$(kubectl version)"
    #- echo "-------->JFROG_VERSION=$(jfrog --version)"
    #- echo "-------->GCLOUD_VERSION=$(gcloud --version)"
    #- echo "-------->ELASTICBEANSEARCH_VERSION=$(eb --version)"
    #- echo "-------->AZURE_VERSION=$(apt search azure-cli)"
    #- echo "-------->AZ_VERSION=$(az --version)"
    #- echo "-------->YARN_VERSION=$(yarn --version)"
    - EXPECTED_DOCKER_VERSION="17.06.0-ce"
    - |
      [ $(docker version --format {{.Server.Version}}) == "$EXPECTED_DOCKER_VERSION" ]
